name = "RNA Secondary Structure Prediction (RiNALMo)"
description = "Predicts RNA secondary structure from a FASTA sequence using the RiNALMo 650M language model with bpRNA fine-tuned weights"

[dependencies]
01-validate-input = ["00-prepare-input"]
02-predict         = ["01-validate-input"]
03-report          = ["02-predict"]
